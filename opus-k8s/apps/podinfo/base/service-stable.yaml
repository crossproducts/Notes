# Stable Service — receives the bulk of traffic. Argo Rollouts flips the
# VirtualService weight between this and the canary Service during a rollout.
apiVersion: v1
kind: Service
metadata:
  name: podinfo-stable
spec:
  selector:
    app: podinfo
  ports:
    - name: http
      port: 9898
      targetPort: 9898
