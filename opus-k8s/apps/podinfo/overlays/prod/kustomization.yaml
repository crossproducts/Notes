apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: podinfo-prod

resources:
  - ../../base
  - httproute.yaml

patches:
  - target: { kind: Rollout, name: podinfo }
    patch: |
      - op: replace
        path: /spec/replicas
        value: 3
