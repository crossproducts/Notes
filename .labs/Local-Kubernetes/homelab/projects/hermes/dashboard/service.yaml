apiVersion: v1
kind: Service
metadata:
  name: hermes-dashboard
  namespace: hermes
  labels:
    app: hermes-dashboard
spec:
  type: ClusterIP
  selector:
    app: hermes-dashboard
  ports:
    - port: 9119
      targetPort: 9119
      protocol: TCP
