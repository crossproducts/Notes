apiVersion: v1
kind: Service
metadata:
  name: hermes-gateway
  namespace: hermes
  labels:
    app: hermes-gateway
spec:
  type: ClusterIP
  selector:
    app: hermes-gateway
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
