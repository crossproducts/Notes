apiVersion: v1
kind: Service
metadata:
  name: kubernetes-mcp-server
spec:
  type: ClusterIP
  selector:
    app: kubernetes-mcp-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
