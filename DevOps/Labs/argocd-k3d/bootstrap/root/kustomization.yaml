apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Hands control over to GitOps via the root Application.
# Apply AFTER bootstrap/argocd (CRDs must be established first).
namespace: argocd

resources:
  - root-app.yaml
