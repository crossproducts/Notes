apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openclaw
spec:
  ingressClassName: traefik
  rules:
    - host: openclaw.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: openclaw
                port:
                  number: 18789
