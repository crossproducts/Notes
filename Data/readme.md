# Data Engineer

→ Transactions → OLTP → Row-based → RDS/DynamoDB
→ Analytics → OLAP → Columnar → Redshift/Athena
→ JSON flexible data → Document → DynamoDB/Mongo
→ Relationships → Graph → Neptune